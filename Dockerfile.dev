FROM golang:1.25-alpine

RUN apk add --no-cache git gcc musl-dev

WORKDIR /app

# Hot reload with air
RUN go install github.com/air-verse/air@latest

COPY go.mod go.sum ./
RUN go mod download

CMD ["air", "--cmd", "go run ./cmd/server", "--", "-b", "0.0.0.0:2785"]
